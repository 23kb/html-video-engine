# Deterministic Logic Findings

Generated by:

```bash
node tools/lint-determinism.js --all
```

Result on 2026-09-18: `0 error(s), 16 warning(s)`. The two `Date.now()`
errors in `iframe-helpers.js` `awaitLayout` were fixed the same day: the
poll now counts wait ticks instead of reading the wall clock.

## Warning Summary

Every warning is a `setTimeout()` in shared library code. All 16 are known
and deliberate: fixed-millisecond waits that stay alive when a backgrounded
preview throttles `requestAnimationFrame` (see the comments at
`narration.js` `wait` and `IframeManager.wait`). None reads the wall clock.

| File | Warnings | What the timers do |
|---|---:|---|
| `videos/_shared/motion-primitives.js` | 8 | `wait()` helper; short settle gaps inside cursor / press sequences |
| `videos/_shared/wpforms-interactions.js` | 6 | `IframeManager.wait()`; drag-drop mid-carry reveal; CSS-transition cleanup; per-character label typing |
| `videos/_shared/narration.js` | 1 | `wait()` — the setTimeout-backed sleep every film awaits |
| `videos/_shared/shorts-kit.js` | 1 | `settle()` resolves after its hold |

The linter has no allow-annotation, so these stay as warnings. A
`// lint-allow: determinism` annotation in `tools/lint-determinism.js` is
the proposed fix; it is a validation-tooling change and waits for approval.

History: the first pass (2026-05) listed 56 warnings, mostly unseeded
`Math.random()` in the retired engine's `runtime/` cinematics
(retired 2026-08-22).

## Follow-Up Shape

- New code: seeded `mulberry32(seed)` for randomness; ctx `sleep()`,
  `pausableSleep()`, or registered timelines for waits that change visible
  timing.
- Re-run `node tools/lint-determinism.js --all` after any shared-library edit.
