# Outline — fixture-admin-page
> Test fixture — not a real snapshot.

## Targets

### Actions (buttons / links)
- `.export-csv` — a — "Export CSV"

### Inputs & controls
- `#goal-input` — input — "Conversion rate goal"

### Tabs & nav
- `#field-table` — table — "Name Type Views"

### Panels & modals
- `#ai-modal` — div — "AI modal" _(hidden)_

### Other anchors
- `.should-not-emit` — div — "other anchors are not starter-grade"

## Interactions — drivable inside the video iframe

_interactivity.js is not linked on this snapshot — no in-page transitions._
